# IP allowlist generators

This repo holds Helm configs that are automatically generated by various services that extract public IPs from various sources (OVH, TSW, AWS, etc) and create ready to use Helm values. To be used by clients in their charts with Argo multi-source repository configs.
